% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability_and_validity.R
\name{analyze_reliability_ttest}
\alias{analyze_reliability_ttest}
\title{分析两个会话的重测信度 (t检验, 支持批量处理)}
\usage{
analyze_reliability_ttest(
  data,
  subject_col,
  session_col,
  value_cols,
  trial_type = c("single", "average"),
  trial_col = NULL,
  method = "mean",
  icc_model = c("twoway", "oneway"),
  icc_type = c("agreement", "consistency"),
  var_names = NULL
)
}
\arguments{
\item{data}{一个长格式的数据框。}

\item{subject_col}{包含被试ID的列名。}

\item{session_col}{包含测试会话/时间点的列名。此列必须恰好包含两个水平。}

\item{value_cols}{包含需要分析的指标数值的列名。**可以是单个字符串，也可以是包含多个字符串的向量**。}

\item{trial_type}{自定义的ICC类型（single/average）。}

\item{trial_col}{(可选) 包含试次编号的列名(如使用平均测试成绩)。}

\item{method}{聚合会话内多项试次的方法。}

\item{icc_model}{(可选) 传递给`ICC`函数的模型（oneway/twoway）。}

\item{icc_type}{(可选) 传递给`ICC`函数的类型（agreement/consistency）。}

\item{var_names}{(可选) 一个与 `value_cols` 一一对应的字符向量，用于在结果中重命名变量。
如果提供，其长度必须与 `value_cols` 相同。}
}
\value{
返回一个数据框，包含一个或多个变量的信度分析结果，并附有t检验和Cohen's d统计量。
}
\description{
专为比较两个会话(test-retest)的场景设计。可对单个或多个因变量进行分析。
  使用配对t检验和Cohen's d评估系统误差。
}
\examples{
# 筛选出前两次会话 (Week1 vs Week2) 进行 t检验信度分析
reliability_subset_ttest <- reliability_data[reliability_data$Session \%in\% c("Week1", "Week2"), ]
analyze_reliability_ttest(
  data = reliability_subset_ttest,
  subject_col = "Subject",
  session_col = "Session",
  value_cols = "Score",
  trial_col = "Trial",
  method = "mean"
)
}
