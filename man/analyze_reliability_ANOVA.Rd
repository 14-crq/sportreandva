% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability_and_validity.R
\name{analyze_reliability_ANOVA}
\alias{analyze_reliability_ANOVA}
\title{从长格式数据分析重测信度 (支持单个或多个变量)}
\usage{
analyze_reliability_ANOVA(
  data,
  subject_col,
  session_col,
  value_cols,
  trial_type = c("single", "average"),
  trial_col = NULL,
  method = "mean",
  icc_model = c("twoway", "oneway"),
  icc_type = c("agreement", "consistency"),
  var_names = NULL
)
}
\arguments{
\item{data}{一个长格式的数据框。}

\item{subject_col}{包含被试ID的列名。}

\item{session_col}{包含测试会话/时间点的列名。建议此列为因子类型以保证顺序正确。}

\item{value_cols}{包含需要分析的指标数值的列名。可以是单个字符串，也可以是包含多个字符串的向量。}

\item{trial_type}{用户选择的ICC类型。当 trial_col 被使用且 method = 'mean' 时，此参数会被自动设为 "average"。}

\item{trial_col}{(可选) 包含试次编号的列名。如果提供，函数会聚合这些试次。}

\item{method}{聚合会话内多次尝试的方法。}

\item{icc_model}{(可选) 传递给`ICC`函数的模型。默认为 "twoway"。可选 "oneway" 或 "twoway"。}

\item{icc_type}{(可选) 传递给`ICC`函数的类型。默认为 "agreement"。可选 "agreement" 或 "consistency"。}

\item{var_names}{(可选) 一个与 `value_cols` 一一对应的字符向量，用于在结果中重命名变量。
如果提供，其长度必须与 `value_cols` 相同。}
}
\value{
返回一个数据框，包含一个或多个变量的会话间信度分析结果。
}
\description{
为长格式数据设计。可对单个或多个因变量进行分析。
  函数会自动聚合一个“会话(Session)”内的多次“尝试(Trial)”，
  或将一个“会话(Session)”作为独立个体，然后计算“会话(Session)”之间的信度。
}
\examples{
# 使用全部三个会话进行 ANOVA 信度分析
analyze_reliability_ANOVA(
  data = reliability_data,
  subject_col = "Subject",
  session_col = "Session",
  value_cols = "Score",
  trial_col = "Trial",
  method = "mean"
)
}
