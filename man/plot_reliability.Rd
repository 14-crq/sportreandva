% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability_and_validity.R
\name{plot_reliability}
\alias{plot_reliability}
\title{从长格式数据可视化多次重复测量差异}
\usage{
plot_reliability(
  data,
  subject_col,
  session_col,
  value_cols,
  var_names = NULL,
  custom_colors = NULL,
  add_significance = TRUE,
  hide_ns = TRUE,
  comparison_method = "t.test",
  paired = TRUE
)
}
\arguments{
\item{data}{一个长格式的数据框(data.frame)。}

\item{subject_col}{包含被试ID的列名。}

\item{session_col}{包含将在X轴上显示的会话/时间点的列名。}

\item{value_cols}{包含将在Y轴上显示的指标数值的列名。可以是单个字符串或字符串向量。}

\item{var_names}{(可选) 一个与 `value_cols` 一一对应的字符向量，用于设定每个图形的Y轴标题。}

\item{custom_colors}{(可选) 一个包含颜色的字符向量，用于自定义每个会話的颜色。}

\item{add_significance}{(可选) 一个逻辑值 (`TRUE`/`FALSE`)，决定是否添加两两对比的显著性检验。默认为 `FALSE`。}

\item{hide_ns}{(可选) 一个逻辑值 (`TRUE`/`FALSE`)。如果为 `TRUE` (默认)，将只显示显著（p < 0.05）的对比连接线。}

\item{comparison_method}{(可选) 一个字符串，指定进行显著性检验的统计方法。默认为 "t.test"。}

\item{paired}{(可选) 一个逻辑值 (`TRUE`/`FALSE`)，指定是否进行配对检验。对于重复测量数据，应设为 `TRUE` (默认)。}
}
\value{
返回一个包含一个或多个 ggplot 对象的列表。
}
\description{
创建一个或多个“云雨图”(Raincloud Plot)，可自动添加各组间的两两比较显著性，
  并可选择性地隐藏不显著（ns）的标记和连接线。
}
